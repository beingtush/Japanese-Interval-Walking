<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!--
          Receives pause / skip / stop from the watch companion.
          Standard flavor only: the Data Layer ships in Google Play services, which the
          F-Droid flavor deliberately excludes.
        -->
        <service
            android:name=".service.WatchControlListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/jiw/control" />
            </intent-filter>
        </service>
    </application>
</manifest>
